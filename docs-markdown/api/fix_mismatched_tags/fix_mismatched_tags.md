# fix_mismatched_tags

**File:** `Application/fix_mismatched_tags.py`

## Description

Fix mismatched pre/div tags in HTML files.

## Functions

### fix_mismatched_tags

```python
def fix_mismatched_tags(html_content)
```

Fix tags where <pre> opens but </div> closes or vice versa.

**Parameters:**

- `html_content`

### process_html_files

```python
def process_html_files(base_dir)
```

Process all HTML files in the directory.

**Parameters:**

- `base_dir`

