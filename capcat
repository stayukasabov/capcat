#!/bin/bash
# Capcat - Minimal Executable Shortcut
# Lightweight bash wrapper that delegates all logic to Python wrapper

# Get script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" &>/dev/null && pwd)"

# Execute Python wrapper with all arguments
exec python3 "${SCRIPT_DIR}/run_capcat.py" "$@"