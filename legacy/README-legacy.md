# Legacy Files

These files are the pre-packaging entry points for Capcat.
Deprecated. Will be removed in a future release.

Use `pip install capcat` and the `capcat` command instead.

| File | Replacement |
|---|---|
| `capcat_legacy.py` (root) | `capcat/cli.py` — kept at root for BRIDGE imports until fully ported |
| `cli.py` | `capcat/cli.py` |
| `run_capcat.py` | `python -m capcat` |
