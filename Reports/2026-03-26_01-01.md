# Session Report — 2026-03-26 01:01

## Goals

- Implement per-source `article_count` system: every source YAML gets its own configurable fetch count
- Resolution order: CLI `--count` > source YAML `article_count` > fallback 30
- Remove dead `default_count` from all bundle code
- Add `article_count` prompt to add-source TUI flow
- Replace flat "List All Sources" view with interactive detail+edit view
- Remove "Generate Custom Source Config" from TUI menu
- Audit and add test coverage for TUI flows untested since pipx migration (Remove, Test a Source, Manage Bundles)
- Bump to v1.5.0

## Accomplished

- **Task 1**: Added `article_count: 30` + explanatory field comments to all 10 config-driven source YAMLs (`bbc`, `bbcsport`, `google-reserch`, `guardian`, `ieee`, `iq`, `mashable`, `mitnews`, `nature`, `scientificamerican`)
- **Task 2**: Added `article_count: 30` + field comments to all 7 custom source configs (`hn`, `lb`, `medium`, `substack`, `twitter`, `vimeo`, `youtube`)
- **Task 3**: Removed dead `default_count` field from `bundles.yml` and 7 bundle code files (`bundle_models.py`, `bundle_manager.py`, `bundle_service.py`, `bundle_ui.py`, `bundle_validator.py`, `config/source_base.py`, `config/source_registry.py`); `validate_default_count` method deleted from `bundle_validator.py`
- **Task 4**: Added `article_count: int = 30` to `SourceConfig` dataclass with `__post_init__` validation (rejects ≤ 0); promoted from YAML in `source_registry.py` for both config-driven and custom sources
- **Task 5**: Changed CLI `--count` default from `"30"` to `None` in both `_cmd_fetch` and `_cmd_bundle`; added `_resolve_count(cli_count, source_config)` helper in `unified_source_processor.py`; `process_source_articles` now accepts `Optional[int]`; fixed `fetch.py` to pass `args.count` directly (removed misleading `getattr(..., 30)`)
- **Task 6**: Added `get_article_count()` to `UserInterface` Protocol + `QuestionaryUserInterface` + `MockUserInterface`; added `article_count: int = 30` to `SourceMetadata`; wired into `_collect_source_metadata`; `article_count` written to generated YAML; removed "Generate Custom Source Config" from TUI menu
- **Task 7**: Replaced flat `_handle_list_sources` with interactive detail+edit flow; added `_show_source_detail` and `_edit_source_count`; `_edit_source_count` handles both config-driven and custom source YAML paths; mirrors builtin to userspace before editing
- **Task 8**: Added audit test coverage for `_handle_remove_source`, `_handle_test_source`, `_handle_manage_bundles` — 8 tests across 3 new files
- Post-review fixes: `fetch.py` dead `getattr` fallback, custom source YAML path in `_edit_source_count`, replaced stub `test_edit_count_writes_to_yaml` with real call to `_edit_source_count`, removed dead code block from remove test, cleaned `default_count` from `MOCK_BUNDLES` in `test_bundle_expansion.py`
- Merged `feat/per-source-article-count` to `main` via `--no-ff`; bumped `__version__` to `1.5.0`; worktree removed

**Final test count:** 377 passed, 0 failures

## Root Causes / Key Findings

- `getattr(args, "count", 30)` in `fetch.py` was dead code — since `args.count` is always present (set by `_pop_value`), the `30` default was never reached. Fixed to `args.count`.
- `_edit_source_count` initially only resolved config-driven paths. Custom sources live at `custom/{source_id}/config.yaml` — second path added with clear error if neither exists.
- `test_edit_count_writes_to_yaml` originally exercised only PyYAML round-trip, not `_edit_source_count` itself. Replaced with a proper call-through test.

## Files Changed

- `capcat/__init__.py` — version bump to 1.5.0
- `capcat/cli.py` — `--count` default `"30"` → `None`; `int(count_str) if count_str is not None else None` guard
- `capcat/commands/fetch.py` — `getattr(args, "count", 30)` → `args.count`
- `capcat/core/source_system/base_source.py` — `article_count: int = 30` added to `SourceConfig`
- `capcat/core/source_system/source_registry.py` — promote `article_count` from YAML in both loaders
- `capcat/core/unified_source_processor.py` — `_resolve_count` helper; `count: Optional[int]`
- `capcat/core/source_system/add_source_command.py` — `SourceMetadata.article_count`; Protocol method; `_collect_source_metadata` wiring
- `capcat/core/source_system/questionary_ui.py` — `get_article_count()` on both UI classes
- `capcat/core/source_system/source_config_generator.py` — `article_count` in generated YAML dict
- `capcat/core/interactive.py` — interactive list+detail+edit; removed "Generate Custom Source Config" menu entry
- `capcat/core/config/source_base.py` — removed `BundleConfig.default_count`
- `capcat/core/config/source_registry.py` — removed `default_count=N` from all `BundleConfig(...)` calls
- `capcat/core/source_system/bundle_*.py` (5 files) — `default_count` removed throughout
- `capcat/sources/builtin/bundles.yml` — `default_count` lines removed; comments added
- `capcat/sources/builtin/config_driven/configs/*.yaml` (10 files) — `article_count: 30` + field comments
- `capcat/sources/builtin/custom/*/config.yaml` (7 files) — same
- `tests/unit/test_article_count_resolution.py` — new: SourceConfig validation + registry loading + `_resolve_count`
- `tests/unit/test_add_source_count_prompt.py` — new: count prompt in add-source flow
- `tests/unit/test_bundle_no_default_count.py` — new: asserts `default_count` is gone
- `tests/unit/test_tui_list_sources.py` — new: detail view + `_edit_source_count` call-through
- `tests/unit/test_tui_remove_source.py` — new: audit for remove flow
- `tests/unit/test_tui_test_source.py` — new: audit for test-source flow
- `tests/unit/test_tui_manage_bundles.py` — new: audit for manage-bundles flow
- `tests/unit/test_bundle_expansion.py` — removed `default_count` from `MOCK_BUNDLES`

## Open TODOs

- [ ] Push to remote: `git push` (main is 5+ commits ahead of origin)
- [ ] Tag and publish: `git tag v1.5.0 && git push origin v1.5.0` (triggers GitHub Actions → PyPI)
- [ ] The broken `from capcat import run_app` import inside `_handle_test_source` was noted but not fixed — `run_app` is not exported from `capcat.__init__`. Currently swallowed by `except Exception`. Should be cleaned up.

## Resume Context

**Branch:** `main`
**State:** All 8 tasks merged, version bumped to 1.5.0. Not yet pushed or tagged.

### To verify current state:
```bash
cd ~/capcat
source venv/bin/activate
pytest tests/unit/ -q
python3 -c "import capcat; print(capcat.__version__)"
git log --oneline -5
```

### Key decisions made this session:

- `_resolve_count` placed in `unified_source_processor.py` (not `fetch.py`) so both fetch and bundle command paths benefit automatically through `process_source_articles`
- `_edit_source_count` uses line-by-line rewrite (not YAML round-trip) to preserve comments in source YAML files
- "Generate Custom Source Config" removed from TUI menu — kept as developer tool in codebase, documented for power users via docs/capcat.org
- Subagent-driven development used: fresh subagent per task + 2-stage review (spec compliance then code quality)

### What to tackle next:

- Push main + tag v1.5.0 to trigger PyPI publish
- Fix `_handle_test_source` broken import (`from capcat import run_app`)
- Consider adding `article_count` display to the `capcat list` CLI command output
